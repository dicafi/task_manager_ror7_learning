<%= link_to("Back to List", categories_path, :class => 'back-link') %>

<div class="edit new">
  <h2>Edit Category</h2>

  <%= form_for(@category) do |f| %>
    <% # url: categories_path(@task), method: 'patch' %>

    <%= render(partial: 'form', locals: { f: f }) %>

    <div class="form-buttons">
      <%= f.submit("Edit Category") %>
    </div>

  <% end %>
</div>
